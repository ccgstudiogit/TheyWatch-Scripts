// While this script does not have any implementations, this script is useful for objects to determine if
// a LevelController is a HM LevelController--any object/script in an environment that needs to do something
// specific for HM should just check if LevelController.instance is IHMLevelController
public interface IHMLevelController { }
